@{
  Layout = "_Layout";
}

@using HairSalon.Models;

<h1>Clients</h1>

@if (@Model.Count == 0)
{
  <h3>No clients have been added yet!</h3>
}
else
{
  @foreach (Client client in Model)
  {
    <h5>@Html.ActionLink($"{client.Name}", "Details", new { id = client.ClientId })</h5>
  }
}

<p>@Html.ActionLink("Add new client", "Create")</p>